import type { Metadata } from "next"

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}

export const metadata: Metadata = {
  title: "设置 - lorem314.io",
}
